{
  "name": "token-police",
  "version": "1.0.0",
  "description": "Local dashboard for tracking per-human-request token usage across Claude Code and Codex CLI sessions",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node start.js",
    "server": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "express": "^4.21.2"
  }
}
